import { PreviewClient } from "./PreviewClient";

export const metadata = { title: "Preview site · ReplacedByAI" };
export const dynamic = "force-dynamic";

export default function Page() {
  return <PreviewClient />;
}
